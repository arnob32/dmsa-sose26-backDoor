spring.application.name=config-server
server.port=8888

# Native profile: serve configuration from the classpath config-repo folder
# instead of a remote Git repository (simplest for local lab use).
spring.profiles.active=native
spring.cloud.config.server.native.search-locations=classpath:/config-repo/
