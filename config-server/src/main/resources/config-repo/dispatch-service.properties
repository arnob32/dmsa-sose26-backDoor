# Centralized configuration for dispatch-service, served by the Config Server.
# These values are externalized here (Task 2) instead of living in the service.

dispatch.info-message=Configuration loaded centrally from Spring Cloud Config Server
dispatch.default-due-days=7
